matrix:
  include: 
    - os: linux
      dist: trusty
      compiler: g++
      jdk: oraclejdk8
    - os: linux
      dist: trusty
      compiler: g++
      jdk: oraclejdk7
    - os: linux
      dist: trusty
      compiler: g++
      jdk: openjdk7
    - os: linux
      dist: trusty
      compiler: g++
      jdk: openjdk6
    - os: linux
      compiler: clang++
# Probably due to 'language' setting below, despite above line, the 'CXX' environment have to be set .
      env: CXX=clang++
    - os: osx
      compiler: clang++  

language: java

script:
- make -j
- java -Djava.library.path=. eSketchDemo